name: PoC zombienet CI

on:
  push:
    branches: [ poc-zombienet ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Get zombienet
        run: |
          curl -L -O https://github.com/paritytech/zombienet/releases/download/v1.2.7/zombienet-linux
          chmod +x zombienet-linux

      - name: Get polkadot
        run: |
          curl -L -O https://github.com/paritytech/polkadot/releases/download/v0.9.15-1/polkadot
          chmod +x polkadot
          ./polkadot --help

      # - name: check podman
      #   run: |
      #     echo "podman version $(podman --version)"
      #     podman info
      #     podman run --rm alpine ls

      - name: Run test
        run: |
          export PATH=$(pwd):$PATH
          echo "running on $(pwd)"
          echo $PATH
          DEBUG=zombie* ./zombienet-linux -p native test ./zombienet-tests/0001-parachains-smoke-test.feature